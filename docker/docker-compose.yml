version: "3.9"

services:

  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ../.env

  langflow:
    image: langflowai/langflow
    ports:
      - "7860:7860"

  chroma:
    image: chromadb/chroma
    ports:
      - "8001:8000"